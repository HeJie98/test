server:
  port: 8890
spring:
  application:
    name: consumer-feign
eureka:
  client:
    serviceUrl:
      defaultZone: http://user:password123@localhost:6767/eureka
logging:
  level:
    root: INFO

